const fs = require('fs'); const content = fs.readFileSync('c:/Users/GANES/Futura-Edtech/src/science/class6/chapter6/MaterialsAroundUs/storyEngine.js', 'utf8'); const lines = content.split('\n'); let hasMissing = false; lines.forEach((line, index) => { if (line.includes('type: \'activity\'') && !line.includes('component:')) { console.log('Line ' + (index + 1) + ': ' + line); hasMissing = true; } }); if (!hasMissing) console.log('All activity nodes have components.');
